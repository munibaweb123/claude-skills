apiVersion: v2
name: example-app
description: "Example application using org-standards library chart"
type: application
version: 1.0.0
appVersion: "1.0.0"

keywords:
  - example
  - demo
  - api

maintainers:
  - name: "Developer"
    email: "dev@company.com"

# Library chart dependency
dependencies:
  - name: org-standards
    version: "1.0.0"
    # In production, use OCI registry:
    # repository: "oci://registry.company.com/helm-charts"

    # For local development:
    repository: "file://../org-standards-chart"
